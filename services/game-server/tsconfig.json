{
  "compilerOptions": {
    /* Configurações da Linguagem e Ambiente */
    "target": "ES2020", // Gera código JavaScript moderno.
    "lib": ["ES2020", "DOM"], // <-- AQUI ESTÁ A SOLUÇÃO! Informa ao TS sobre as bibliotecas do ES2020.
    "module": "commonjs", // Usa o sistema de módulos do Node.js.
    "esModuleInterop": true, // Permite compatibilidade entre diferentes tipos de módulos.
    "forceConsistentCasingInFileNames": true, // Garante que os nomes dos arquivos sejam consistentes.

    /* Verificação de Tipos Estrita */
    "strict": true, // Habilita todas as opções de verificação estrita.
    "skipLibCheck": true, // Pula a verificação de tipos dos arquivos de declaração (.d.ts).

    /* Saída */
    "outDir": "./dist", // Onde o código JavaScript compilado será salvo.
    "rootDir": "./src" // Onde está o nosso código TypeScript.
  },
  "include": [
    "src/**/*" // Quais arquivos o compilador deve processar.
  ],
  "exclude": [
    "node_modules",
    "**/*.test.ts" // Exclui os arquivos de teste da compilação final.
  ]
}