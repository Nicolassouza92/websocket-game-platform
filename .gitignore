# Arquivo .gitignore para o projeto de plataforma de jogos

# Dependências e arquivos de build
# A pasta node_modules é gerada pelo 'npm install' e pode ser muito grande.
# Cada desenvolvedor deve gerá-la localmente.
node_modules/
dist/
build/

# Logs
# Arquivos de log gerados pela aplicação ou por ferramentas.
*.log
logs
*.log*
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Arquivos de ambiente
# Contém segredos e configurações locais (senhas, chaves de API).
# NUNCA deve ser comitado no Git. Cada desenvolvedor cria o seu.
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Arquivos de cache e temporários de editores/SO
# Lixo gerado pelo sistema operacional ou pelo editor de código.
.DS_Store
Thumbs.db
.cache/
.tmp/
.vscode/ # Opcional, alguns preferem compartilhar configurações de workspace.

# Arquivos de teste e cobertura de código
coverage/
.jest-cache/

# Outros
.idea/ # Para usuários do JetBrains IDEs (WebStorm, etc.)
letsencrypt